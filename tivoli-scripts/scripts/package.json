{
	"scripts": {
		"files": "copyfiles -u 1 src/assets/**/* ../dist/tivoli",
		"minify": "terser --config-file terser.json ../dist/tivoli.js -o ../dist/tivoli.js",
		"build": "npm run files && tsc",
		"start": "npm run files && tsc -w"
	},
	"dependencies": {
		"copyfiles": "^2.3.0",
		"rimraf": "^3.0.2",
		"terser": "^4.7.0",
		"typescript": "^3.9.5"
	}
}
